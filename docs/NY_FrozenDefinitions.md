# تعریف‌های مکانیکی فریزشده — NY Box (فازِ ۱، منطقِ فریزشده v1.5 + ابزاریِ قدمِ ۳ v1.6)

> منبع: `Spec_NY_Standalone_v1.md` (دستورکارِ اصلی از مدیرِ پروژه) + `NY_Box_Backtest_v2.xlsx`
> (بکتستِ چشمیِ ۳ماهه‌ی محمود، ژوئن–اوت ۲۰۲۶، ۶۰ روز × ۳ پنجره‌ی کاندید، `docs/GroundTruth/`).
> این سند تعریف‌های سندِ اصلی را عیناً کپی می‌کند + جاهایی که سند به یک انتخابِ پیاده‌سازی نیاز
> داشت (و متن آن را صریح مشخص نکرده بود) را با عنوانِ «تصمیمِ پیاده‌سازی» مستند می‌کند — طبقِ همان
> روشِ پروژه‌ی توکیو (`RiskManager-EA/docs/DayBias_FrozenDefinitions.md`).

> **به‌روزرسانیِ v1.1** (بعدِ اولین اجرایِ واقعی در MT5 + قدمِ ۲ رویِ پنجره‌ی ۰۸۳۰-۰۹۳۰): فیکسِ
> «MFE/MAE کپ‌شده در لحظه‌ی خروج» (تصمیمِ ۵ زیر) + یک باگ در خودِ `compare_ground_truth.py`
> (رشته‌ی `"None"` را pandas پیش‌فرض NaN می‌خواند).
>
> **به‌روزرسانیِ v1.2** (بعدِ دومین اجرایِ واقعی، این‌بار هر سه پنجره‌ی واقعاً متفاوت): دو یافته —
> (۱) کپ‌شدنِ MFE/MAE در v1.1 رویِ *هر دو* استاپ و TP بود؛ نمونه‌ها نشان دادند ground truth بعدِ TP
> هم به ردیابیِ MFE ادامه می‌داده (فقط بعدِ استاپ متوقف می‌شده) — تصمیمِ ۵ اصلاح شد: فقط استاپ
> freeze می‌کند. (۲) اختلافِ اندازه‌ی باکس با XChief توضیح داده شد (بروکرِ متفاوت با اکسپورتِ MT5،
> نه باگ).
>
> **به‌روزرسانیِ v1.3** (بعدِ بررسیِ دقیق‌ترِ سؤالِ بازِ ۲): محمود پیشنهاد داد مهلتِ ثابتِ ۶۰دقیقه‌ایِ
> پس از بسته‌شدنِ باکس کلاً حذف شود و به‌جایش فقط شکست‌هایِ **ظرفِ یک ساعتِ پایانی پیش از EOD**
> نامعتبر باشند. قبل از پیاده‌سازی، با دیتایِ واقعی سنجیده شد: از ۴۴ روزِ نامنطبقِ قبلی (شکستِ دیرتر)،
> فقط ۱ مورد ظرفِ ساعتِ پایانی بود — یعنی این قاعده ۴۳ از ۴۴ مورد را برمی‌گرداند. علاوه بر این، چک
> شد که آیا این ۴۶ روزِ نامنطبق میانگینِ R مثبت داشتند (سوگیریِ خوش‌بینانه در ground truth) یا نه —
> جواب مثبت بود (میانگین +۰.۲۸R)، که یعنی نادیده‌گرفتنِ این تصمیم واقعاً باعثِ خوش‌بینیِ کاذب می‌شد.
> قاعده‌ی جدید پیاده‌سازی شد (`NY_DetectBreak` حالا `eodInstant` می‌گیرد، نه `boxCloseInstant`).
>
> **به‌روزرسانیِ v1.4 (بزرگ‌ترین فیکس — بعدِ اعتبارسنجیِ هم‌فیدِ XChief طبقِ دستورِ مدیرِ پروژه):**
> اکسپورتِ XChief آمد و `DayNet_R` تقریباً هیچ تغییری نکرد (همچنان ۴۶-۶۰٪ — نه ≥۸۰٪ی که هدف بود) —
> یعنی فرضیه‌ی «اختلافِ بروکر مقصر است» رد شد. ریشه‌یابیِ دقیق‌تر (تفکیکِ خطا بر اساسِ نتیجه‌ی روز)
> نشان داد خطا تقریباً کاملاً در روزهایی متمرکز بود که نه استاپ نه TPِ کامل خوردند («روزهایِ دیگر»):
> فرمولِ قبلی `exitR` را از `EOD_Boxes` (قیمتِ کلوزِ پایانِ روز) می‌ساخت، اما `Reward_R` برگه‌ی چشمی
> برایِ این روزها معلوم شد دقیقاً `MFE_Boxes/۰.۷۵` است (بیشینه‌ی حرکتِ موافقِ همان روز). تأییدِ آماری:
> با فرمولِ EOD تطابق ۱۶٪ بود (n=۶۳ رویِ هر سه پنجره)، با فرمولِ MFE به ۸۱٪ رسید. بعدِ این فیکس،
> نرخِ کلیِ تطابقِ `DayNet_R` (شبیه‌سازیِ آفلاین رویِ دیتایِ موجود) به **۷۴-۹۴٪** رسید (پیش‌تر ۴۶-۶۰٪).
> **قدمِ ۲ تمام است؛ فقط یک اجرایِ تازه با کدِ v1.4 لازم است تا این عدد رسماً با MT5 تأیید شود.**
>
> **به‌روزرسانیِ v1.5 (تصحیحِ v1.4 — بعدِ کشفِ محمود از یک ستونِ گم‌شده در فایلِ چشمی):** محمود
> اشاره کرد به یک ستونِ سیزدهم در `NY_Box_Backtest_v2.xlsx` («final result at: 23:50») که استخراجِ
> اولیه‌ی من (همان اول کار) اصلاً نگرفته بود — فقط ۱۲ از ۱۳ ستون کپی شده بود. بررسی نشان داد این
> ستون **دقیقاً همان چیزی است که باید از اول با آن `DayNet_R` را می‌سنجیدم**، نه `Reward_R`:
> - `Reward_R` (ستونِ ۱۱) = «سقفِ MFE» (تا کجا می‌شد رسید) = `MFE_Boxes/۰.۷۵`.
> - ستونِ ۱۳ («نتیجه‌ی نهایی در ۲۳:۵۰») = نتیجه‌ی *واقعیِ* معامله در کلوزِ EOD، فقط برایِ روزهایِ
>   نه‌استاپ-نه‌TP پر شده (۷۶ از ۷۶ چنین روزی؛ صفر برایِ روزهایِ استاپ/TP — چون آنجا `Reward_R`
>   خودش (-1 یا 2.667) کاملاً نتیجه‌ی واقعی را نشان می‌دهد).
>
> یعنی v1.4 اشتباه بود: فرمولِ اصلیِ EOD-based (v1.1-v1.3) از اول درست بوده، فقط با ستونِ غلط
> (Reward_R) سنجیده می‌شد. v1.5: فرمولِ EOD-based برگشت (`exitR`)؛ فرمولِ MFE-based در یک فیلد/ستونِ
> جدا (`potentialR` / `DayNet_R_Potential` در CSV) نگه داشته شد — هر دو مفهوم واقعی و مفیدند، فقط
> نباید با هم قاطی شوند. تأییدِ آماری با ground truthِ درست: `DayNet_R` رسمی حالا **۸۷٪/۹۲٪/۸۶٪**
> (هر سه پنجره، `compare_ground_truth.py`) — بالایِ هدفِ ۸۰٪ی مدیرِ پروژه، رویِ هر دو دیتاستِ
> XChief/Errante. **تأییدِ رسمی با اجرایِ واقعیِ MT5 هم بعداً انجام شد و دقیقاً همین اعداد را داد
> (نگاه کن به «نتایجِ دورِ ششم» پایینِ سند).**
>
> **به‌روزرسانیِ v1.6 (۲۳ اوت — تصمیم‌هایِ مدیرِ پروژه برایِ اجرایِ قدمِ ۳ی موقت،
> `Manager_Decision_20260823.md`):** ادامه‌ی کار تا رسیدنِ دیتایِ کاملِ SpreadLog/NewsDay تأیید
> شد، با قیدهایِ زیر — **تعریف‌هایِ فریزشده‌ی v1.5 (بالا) دیگر دست نمی‌خورند**؛ این‌ها فقط
> اضافه/ابزاری‌اند:
> 1. **`NY_STOP_DEPTH_PCT` پارامتریک شد** (`InpStopDepthPct` در `NY_DataScript.mq5`، پیش‌فرض
>    همان ۰.۷۵ی فریزشده) — برایِ جاروی سؤالِ ۲ (۰.۵/۰.۶/۰.۷۵)، بدونِ تغییرِ رفتارِ پیش‌فرض.
> 2. **`NewsDay` دیگر نیازمندِ فایلِ خارجی نیست** — پیش‌فرض از تقویمِ اقتصادیِ داخلیِ MT5
>    (`CalendarValueHistory`, currency=USD, importance=HIGH) پر می‌شود؛ فایلِ دستی فقط پلن B
>    است (اگر عمقِ تاریخیِ تقویمِ ترمینال کافی نبود — اسکریپت این را در لاگ چک و گزارش می‌کند).
> 3. **مدلِ هزینه، موقت:** تا رسیدنِ SpreadLog کامل، هزینه‌یِ ثابتِ **۰.۰۶R به‌ازایِ هر لگِ اجراشده**
>    اعمال و در گزارشِ موقت جدا از عددِ خام گزارش می‌شود (نه اعمال روی خودِ CSVها — در تحلیل).
>    معماریِ نهایی سه‌جزئی خواهد بود: اسپرد (از SpreadLog)، کمیسیون (از قراردادِ بروکر)، اسلیپیج
>    (از ممیزیِ حساب ریل، نه دمو — دموی XChief/Errante اسلیپیجِ کاذبِ صفر نشان می‌دهد؛ برآوردِ
>    اولیه از یک ممیزیِ MT4 جدا: میانه ۰.۰۲۹R، میانگین ۰.۰۷۲R، به‌تفکیکِ ساعت — پنجره‌ی ۸:۳۰-۱۰:۰۰NY
>    گران‌ترین ساعتِ اسلیپیج در آن ممیزی بود؛ سناریوهایِ P75/P95 باید این را لحاظ کنند).
> 4. **قابِ تفسیرِ معمای ریورس:** دیاگنوستیکِ ۱۵روزه منطقِ موتور را تأیید کرد. فرضیه‌یِ پیش‌فرض
>    برایِ فاصله‌یِ +۰.۷۵R (چشمی، نمونه‌ی کوچک) در برابرِ -۰.۰۳R (مکانیکی، کلِ ۱۷ماهه): **سوگیریِ
>    ثبتِ انتخابی در برگه‌ی چشمی** (فقط ریورس‌هایِ چشمگیر لاگ شده‌اند، نه همه‌یِ فعال‌سازی‌ها).
>    تحلیلِ کاملِ قدمِ ۳ باید این را صریح جدا گزارش کند: E ریورس رویِ **همه‌یِ فعال‌سازی‌ها** +
>    توزیعِ `Rev_ExitR`. اگر تأیید شد، این سومین ابطالِ تمیزِ پروژه است (نتیجه‌ی معتبر، نه شکست).
> 5. **خروجیِ این مرحله:** `NY_Standalone_Report_PRELIM.md` — با برچسبِ صریحِ «موقت»، هیچ عددی وارد
>    حکمِ نهایی نمی‌شود تا SpreadLog کامل + NewsDay برسند.

## جایگاه

استراتژیِ مستقلِ NY، روی همان خطِ تولیدِ توکیو (ماژول‌های مشترک: `RM_SessionTime.mqh`،
`NY_BoxLayer.mqh` = پورتِ عینیِ `DayBias_BoxLayer.mqh`). ترکیب/جانشینی با توکیو = فازِ ۲ (بعد از
این، دستورِ جدا). این فاز فقط اسکریپتِ دیتا + تحلیلِ آفلاین است — **هیچ EA ساخته نمی‌شود.**

## تعریف‌های فریزشده (از بکتستِ چشمی — طبقِ سند، تغییر ممنوع)

- **باکس:** سه پنجره‌ی کاندید به وقتِ NY: `۸:۳۰-۹:۳۰` / `۹:۰۰-۱۰:۰۰` / `۹:۳۰-۱۰:۳۰`. پارامتریک
  از طریقِ ورودی‌هایِ `InpBoxStartH/M`, `InpBoxEndH/M` در `NY_DataScript.mq5` — هر اجرا یک پنجره،
  یک `NY_History_<window>.csv` جدا. باکس دوسر بسته است (همان تعریفِ v4ِ توکیو — کندلی که
  `open` آن == BoxEnd هم عضوِ باکس است)، چون `NY_BoxLayer.mqh` عیناً همان `BL_ComputeSessionRange`
  با `inclusiveEnd=true` است.
- **شکستِ معتبر (v1.3، اصلاح‌شده — نگاه کن به «حل‌شده — سؤالِ بازِ ۲» پایینِ سند):** اولین کلوزِ M5
  بیرونِ لبه، بدنه ≥۵۰٪ رنجِ کندل — در هر لحظه از روز معتبر است **جز اگر ظرفِ یک‌ساعتِ پایانی پیش از
  EOD رخ دهد**. اگر ظرفِ آن ساعتِ پایانی بود = روز None برایِ ترید (ستونِ `BreakDir=None`)، اما ساعتِ
  شکست همچنان در `LateBreakTime` ثبت می‌شود. (نسخه‌ی اولیه یک مهلتِ ثابتِ ۶۰دقیقه‌ای *پس از* بسته‌شدنِ
  باکس داشت که با ground truth جور درنمی‌آمد؛ محمود این قاعده‌ی جدید را پیشنهاد داد.)
- **ورود:** لیمیت رویِ لبه‌ی شکسته، اعتبار ۶۰ دقیقه از کلوزِ شکست. پر نشد → روزِ بی‌ترید
  (`DayNet_R` خالی). **این تفاوتِ کلیدی با توکیوست** — توکیو فوراً از کندلِ شکست وارد می‌شود، NY
  منتظرِ ریتست می‌ماند.
- **استاپ:** عمقِ ۷۵٪ ارتفاعِ باکس از لبه، به‌سمتِ داخلِ باکس (پایه — جاروی ۵۰/۶۰/۷۵ فقط در تحلیلِ
  آفلاینِ قدمِ ۳، نه در خودِ اسکریپتِ دیتا).
- **TP:** ۲ باکس از لبه. **بدونِ BE** — هیچ‌جایِ سند BE برایِ NY تعریف نشده، پس اسکریپت هم پیاده‌سازی‌اش
  نمی‌کند.
- **ریورس (فقط برایِ مطالعه‌ی ۸:۳۰):** اردرِ استاپ رویِ لبه‌ی مقابل، با ریسکِ خودش (استاپ ۷۵٪ از
  لبه‌ی خودش، TP=۲باکس)، **لغو در +۱باکسِ لگِ مستقیم**. مکانیزم مستقل از پنجره است (کدِ مشترک)، اما
  طبقِ سند فقط تحلیلِ ۸:۳۰ باید آن را گزارش کند.
- **EOD:** ۲۳:۵۰ **سرور** (نه NY) — قیدِ مطلقِ ویکند به این معنی است که این مرز هیچ‌وقت خودش را به
  شنبه/یکشنبه نمی‌کشاند، چون باکس‌ها همه صبحِ NY هستند و ۲۳:۵۰ سرور همان روزِ تقویمیِ سرور می‌ماند.
- **بدونِ فیلترِ جهت:** Bias (EMA20/H1 در اپنِ هر باکس) و News فقط log-only‌اند — در `NY_BiasLayer.mqh`
  محاسبه می‌شوند اما هیچ اثری روی شکست/ورود/خروج ندارند.

## ستون‌هایِ CSV (`NY_History_<window>.csv`)

ستون‌هایِ اصلی طبقِ متنِ سند («قرینه‌ی برگه‌ی چشمی»): `BoxSize, BreakDir, BreakTime, BreakDist,
RetestWithin60, FillTime, MFE_Boxes, MAE_Boxes, EOD_Boxes, Rev_Triggered, Rev_MFE, Rev_Outcome,
Bias_Daily(log), EMA_Slope(log), NewsDay(log), DayNet_R`.

چند ستونِ تشخیصیِ اضافه (بدونِ اثر روی منطق — همان قراردادِ `BoxStart_Server`/`BoxEnd_Server`ِ
توکیو v3):

| ستون | توضیح |
|---|---|
| `Window` | برچسبِ پنجره (`InpWindowLabel`) — برایِ concat کردنِ سه CSV در تحلیلِ قدمِ ۳ |
| `LateBreakTime` | ساعتِ شکست اگر ظرفِ ساعتِ پایانیِ پیش از EOD بود (وگرنه خالی) |
| `DayNet_R_Potential` | سقفِ MFE-based (`مقایسه با Reward_R`) — تشخیصی، نه ستونِ رسمی؛ نگاه کن به تصمیمِ ۶ |
| `DayNet_R_MarketEntry` | فرضیه‌ی جایگزینِ ورود (بلافاصله در کلوزِ شکست) — تشخیصی |
| `BoxStart_Server` / `BoxEnd_Server` | لنگرهایِ واقعیِ باکس به وقتِ سرور (دقتِ ثانیه)، برایِ راستی‌آزماییِ مستقیم رویِ چارت |

## تصمیم‌هایِ پیاده‌سازی (سند صراحتاً مشخص نکرده بود)

۱. **MFE_Boxes/MAE_Boxes از لحظه‌ی شکست اندازه‌گیری می‌شوند، نه از پرشدنِ لیمیت** (مستقلِ از اینکه
   ورود اصلاً پر شده یا نه). دلیل: در نمونه‌هایِ برگه‌ی چشمی، حتی ردیف‌هایی که `Retest=No` بودند هم
   `MFE_Boxes` مقداردار داشتند.

۲. **هم‌زمانیِ استاپ/TP در یک کندل → استاپ برنده است** (محافظه‌کارانه، مثلِ `LP_CheckLeg`ِ توکیو
   که نمی‌تواند ترتیبِ درون‌کندلی را از OHLC اثبات کند).

۳. **هم‌زمانیِ لغوِ ریورس (+۱باکسِ لگِ مستقیم) و لمسِ لبه‌ی مقابل در یک کندل → لغو برنده است.**
   دلیل: اردرِ ریورس دقیقاً یک باکس آن‌طرف‌تر از لبه‌ی مستقیم می‌نشیند؛ اگر لگِ مستقیم هم‌زمان به
   +۱باکس برسد، محافظه‌کارانه فرض می‌کنیم لگِ مستقیم زودتر رسیده (تا اردرِ ریورس کاذب باز نشود).

۴. **`Rev_MFE_Boxes` منفی یعنی لگِ ریورس هم استاپ خورد** — طبقِ یادداشتِ خودِ برگه‌ی چشمی
   («منفی = لگِ معکوس هم شکست»). مقدار = `-MAE_Boxes` لگِ ریورس در آن حالت.

۵. **MFE_Boxes/MAE_Boxes فقط در لحظه‌ی استاپ freeze می‌شوند — نه در TP، نه تا EOD.** (v1.2 —
   دو مرحله‌ای تصحیح شد.) نسخه‌ی اولیه تا EOD ادامه می‌داد؛ در قدمِ ۲ معلوم شد این با ground truth
   جور درنمی‌آید (محمود بعدِ نتیجه‌گیریِ روز دیگر چارت را دنبال نمی‌کرده). v1.1 هر دو رویدادِ خروج
   (استاپ و TP) را freeze کرد — MAE خیلی بهتر شد (میانگینِ اختلاف از ۱.۲۲ به ۰.۱۰ باکس رسید) اما
   MFE بدتر شد (تطابق از ۸۰٪ به ۷۴٪ افتاد)، چون نمونه‌ها نشان دادند ground truth حتی بعدِ TP هم
   ادامه‌ی حرکتِ روز را ثبت می‌کرده (مثلاً `۲۰۲۶-۰۷-۱۳`: `MFE_gt=۶.۵` باکس، بسیار فراتر از TPِ
   ۲باکس). نتیجه‌ی v1.2: فقط استاپ (ضرر) تماشا را متوقف می‌کند؛ بعدِ TP (سود) ردیابیِ MFE/MAE ادامه
   دارد تا EOD یا تا استاپ (هرکدام زودتر).

۶. **`DayNet_R` (ستونِ رسمی) از رویِ کلوزِ EOD ساخته می‌شود؛ `DayNet_R_Potential` (تشخیصی) از رویِ
   `MFE_Boxes/۰.۷۵`.** (v1.4 اشتباه بود، v1.5 تصحیحش کرد — تاریخچه‌ی کامل بالای سند.) خلاصه: برگه‌ی
   چشمی دو ستونِ *مجزا* دارد — `Reward_R` (سقفِ MFE-based، «تا کجا می‌شد رسید») و یک ستونِ سیزدهمِ
   بی‌نام («final result at: 23:50»، نتیجه‌ی واقعیِ کلوزِ EOD، فقط برایِ روزهایِ نه‌استاپ-نه‌TP پر
   شده). این ستونِ سیزدهم در استخراجِ اولیه‌ی ground truth جا افتاده بود (محمود پیدایش کرد). با
   ground truthِ درست: فرمولِ EOD-based (`DayNet_R`) تطابقِ ۸۲.۷٪ دارد (بالایِ هدفِ ۸۰٪)، و فرمولِ
   MFE-based (`DayNet_R_Potential`) با `Reward_R` تطابقِ ۸۱-۹۴٪ دارد — هر دو معتبرند، برایِ دو سؤالِ
   متفاوت: `DayNet_R` = «استراتژی واقعاً چقدر گرفت»، `DayNet_R_Potential` = «حداکثرِ ممکن چقدر بود».

۷. **`EODResult_Boxes` در `docs/GroundTruth/*.csv` (ستونِ سیزدهمِ اکسل) واحدش باکسِ خام است، نه R.**
   تأییدِ آماری: `EOD_Boxes` مکانیکی (بدونِ تقسیم بر ۰.۷۵) با این ستون ۷۶-۹۷٪ تطابق دارد؛ تقسیم‌کردنش
   بر ۰.۷۵ تطابق را خراب می‌کند. برایِ تبدیل به R (سنجش با `DayNet_R`) باید *بعد از* تطبیق تقسیم شود.

## سؤالِ باز — قبل از اعتمادِ کامل به قدمِ ۲ باید حل شود

در چند ردیفِ نمونه از `docs/GroundTruth/Box_0900-1000.csv` (مثلاً `2026-08-20`)، ستونِ `Retest`
برابرِ `No` بود اما `Reward_R` مقداردار (`2.667`، یعنی TP کاملِ رسیده) — درحالی‌که طبقِ تعریفِ
فریزشده‌ی سند («پر نشد، روز بی‌ترید»)، انتظار می‌رفت این ردیف‌ها `DayNet_R` خالی داشته باشند.

دو تفسیرِ ممکن:
- **الف)** پاسِ چشمیِ محمود واقعاً مدلِ لیمیت-منتظرِ-ریتست را دنبال نکرده — فرضِ ساده‌ترِ «ورود
  بلافاصله در کلوزِ شکست» را برایِ محاسبه‌ی `Reward_R` استفاده کرده، و ستونِ `Retest` صرفاً یک
  یادداشتِ تشخیصی (مثلِ `RetestTouch_Time` توکیو) بوده، نه گیتِ ورود.
- **ب)** تعریفِ سند برایِ اسکریپتِ مکانیکی درست است و این ردیف‌ها دقیقاً همان «اختلافِ سیستماتیک»یِ
  موردِ انتظارِ قدمِ ۲ هستند که باید متوقف و ریشه‌یابی شوند.

برایِ اینکه اسکریپت بدونِ حدسِ کورکورانه هر دو فرضیه را قابلِ آزمایش نگه دارد، ستونِ `DayNet_R`
دقیقاً طبقِ متنِ سند (گیت‌شده با پرشدنِ لیمیت) پر می‌شود، و ستونِ تشخیصیِ `DayNet_R_MarketEntry`
(ورودِ بلافاصله در کلوزِ شکست، بدونِ گیت) هم همیشه محاسبه می‌شود. `tools/compare_ground_truth.py`
هر دو را با `Reward_R` می‌سنجد و گزارش می‌دهد کدام‌یک نرخِ تطابقِ بالاتری دارد — **این خودش جوابِ
تجربیِ سؤال را می‌دهد**، بدونِ نیاز به حدسِ من یا تغییرِ عقب‌گردِ منطق. **حل شد (v1.5):** اصلِ خودِ
سؤال بر مبنایِ فرض غلط بود — `Reward_R` اصلاً معیارِ درستی برایِ سنجیدنِ `DayNet_R` نبود (سقفِ
MFE-based است، نه نتیجه‌ی واقعی). با کشفِ ستونِ واقعیِ ground truth («final result at: 23:50»)،
مدلِ لیمیتِ گیت‌شده (طبقِ متنِ سند) تأییدِ آماریِ قوی گرفت (۸۷-۹۲٪) — نگاه کن به بخشِ «حل‌شده —
سؤالِ بازِ ۲» و تصمیمِ پیاده‌سازیِ ۶.

## حل‌شده — اختلافِ کوچکِ اندازه‌ی باکس (Box_Size)

بررسیِ کدِ SOB در `ExecutiveHub_v1.14.mq4` (فایلِ Hub که محمود فرستاد) نشان داد منطقِ محاسبه‌ی باکس
(کندلِ `open==boxStart`/`open==boxEnd` هر دو عضوِ باکس، فرمولِ DST/آفستِ سرور در `RM_SessionTime.mqh`)
**بایت‌به‌بایت با `NY_BoxLayer.mqh` یکسان است** — باگی در محاسبه نبود. علتِ واقعیِ اختلافِ کوچکِ
اندازه‌ی باکس (میانه ~۰.۴-۰.۷٪، بدونِ جهتِ ثابت): بکتستِ چشمی رویِ بروکرِ **XChief** گرفته شده بود،
اکسپورتِ MT5 رویِ بروکرِ **Errante** — دو فیدِ قیمتِ متفاوت، اختلافِ جزئیِ High/Low کاملاً طبیعی است.
این دیگر جزوِ نگرانی‌ها نیست؛ فقط باید حواسمان باشد قدمِ ۲ (و تحلیلِ نهاییِ قدمِ ۳) با همین سطحِ
تلورانس (±۱-۲٪ رویِ Box_Size، ±۰.۱۵ باکس رویِ MFE/MAE) خوانده شود، نه انتظارِ تطابقِ ۱۰۰٪.

## حل‌شده — سؤالِ بازِ ۲ (مهلتِ شکستِ معتبر) + سؤالِ بازِ ۱ (DayNet_R گیت‌شده/ورودِ بلافاصله)

در قدمِ ۲ (دورِ دوم)، تقریباً **همه‌ی** روزهایی که مکانیکی `BreakDir=None` داد ولی ground truth یک
جهت ثبت کرده بود، دقیقاً همان روزهایی بودند که یک شکستِ واقعی رخ داده ولی *دیرتر از ۶۰ دقیقه* پس
از بسته‌شدنِ باکس بوده. این الگو با فاصله‌ی باکس از اپنِ NY تشدید می‌شد:

| پنجره | نرخِ None-با-وجودِ-جهتِ-gt |
|---|---|
| ۰۸۳۰-۰۹۳۰ | ۸/۶۰ (۱۳٪) |
| ۰۹۰۰-۱۰۰۰ | ۱۷/۶۰ (۲۸٪) |
| ۰۹۳۰-۱۰۳۰ | ۲۱/۶۰ (۳۵٪) |

**تصمیمِ اولِ محمود:** «ملاک محاسباتِ ماشینی است، چون چشمِ انسانی می‌تواند خطا بدهد» — با این تصمیم
دو نکته روشن شد:
۱) محمود توضیح داد در پاسِ چشمی محدودیتِ زمانی اصلاً یکسان اعمال نشده بود (فقط برایِ ۰۹۰۰-۱۰۰۰،
   نه ۰۸۳۰-۰۹۳۰) — خودِ ground truth ناهماهنگ بوده.
۲) قبل از نهایی‌کردنِ تصمیم، این ۴۶ روزِ نامنطبق از نظرِ Reward_R چک شد: **میانگین +۰.۲۸R، ۳۳ برد
   در برابرِ ۱۳ باخت** — یعنی نادیده‌گرفتنِ محدودیتِ زمانی در پاسِ چشمی باعثِ سوگیریِ خوش‌بینانه
   شده بود (روزهایِ برنده‌ای که طبقِ تعریفِ فریزشده اصلاً نباید ترید می‌شدند، حساب شده بودند).

**تصمیمِ نهایی (پیشنهادِ محمود، v1.3):** به‌جایِ مهلتِ ثابتِ ۶۰دقیقه‌ای *پس از بسته‌شدنِ باکس*،
شکست در هر لحظه از روز معتبر است **جز اگر ظرفِ یک‌ساعتِ پایانی پیش از EOD (۲۳:۵۰ سرور) رخ دهد**.
قبل از پیاده‌سازی با دیتایِ واقعی سنجیده شد: از ۴۴ روزِ نامنطبقِ دارایِ `LateBreakTime`، فقط **۱ مورد**
ظرفِ یک‌ساعتِ پایانی بود (میانه‌ی فاصله تا EOD در بقیه: ~۴.۵ ساعت) — یعنی این قاعده تقریباً کاملِ
سوگیری را برطرف می‌کند بدونِ اینکه محدودیتِ منطقیِ «وقتِ کافی برایِ رسیدن به TP» را از دست بدهد.
پیاده‌سازی شد (`NY_DetectBreak` حالا `eodInstant` می‌گیرد؛ نگاه کن به `NY_DetectionLayer.mqh`).

همین اصل («ملاک، تعریفِ فریزشده است») به سؤالِ بازِ ۱ هم اعمال می‌شود: چون سند صراحتاً «پر نشد =
بی‌ترید» را فریز کرده، `DayNet_R` (نسخه‌ی گیت‌شده‌یِ لیمیت) ستونِ رسمی می‌ماند؛
`DayNet_R_MarketEntry` فقط تشخیصی است.

نتیجه: قدمِ ۲ عملاً **تمام شد**. **قدمِ بعدی: یک اجرایِ تازه با کدِ v1.3 (هر سه پنجره)، سپس اجرایِ
دوباره‌یِ `compare_ground_truth.py` برایِ دیدنِ نرخِ تطابقِ واقعیِ بعدِ این دو فیکس.**

## نتایجِ اولین اجرایِ واقعی در MT5 (دورِ اول، قدمِ ۲، فقط پنجره‌ی ۰۸۳۰-۰۹۳۰)

⚠️ **هر سه فایلِ اولین اجرا (`NY_History_08300930.csv`, `NY_History_09301030.csv`,
`NY_History_9001000.csv`) عملاً یک دیتا بودند** — فقط `InpWindowLabel` بینِ سه اجرا عوض شده بود،
`InpBoxStartH/M`/`InpBoxEndH/M` هر سه بار رویِ دیفالت (۸:۳۰-۹:۳۰) مانده بود (`BoxStart_Server`/
`BoxEnd_Server` هر سه فایل دقیقاً `۱۵:۳۰`/`۱۶:۳۰` سرور بود). فقط فایلِ `0830-0930` معتبر بود؛
پنجره‌هایِ ۰۹۰۰-۱۰۰۰ و ۰۹۳۰-۱۰۳۰ هنوز باید واقعاً اجرا شوند (با اینپوت‌هایِ درست، نه فقط برچسب).

قدمِ ۲ رویِ همان فایلِ معتبر (بعدِ فیکسِ باگِ pandas و فیکسِ MFE/MAE-capping بالا):

- **BreakDir:** ۵۰/۶۰ (۸۳٪) — دو موردِ نامنطبق (`۲۰۲۶-۰۶-۱۲`, `۲۰۲۶-۰۷-۲۱`) جهتِ برعکس نشان می‌دادند
  ولی **نتیجه‌ی نهایی (`Reward_R=-1`) در هر دو منبع یکسان بود** — یعنی احتمالاً کندلِ شکست خیلی
  پرنوسان بوده (هر دو لبه را نزدیکِ هم لمس کرده) و انتخابِ «اولین جهت» بینِ چشمِ محمود و منطقِ
  مکانیکی فرق کرده، نه یک باگِ واقعی. بقیه (۸ مورد) روزهایی بودند که مکانیکی `None` داد ولی محمود
  جهتی ثبت کرده بود — این‌ها را باید رویِ چارتِ زنده چک کرد.
- **MFE_Boxes:** بعدِ فیکسِ v1.1، انتظارِ تطابقِ بسیار بهتری می‌رود (قبلاً بخشِ زیادیِ اختلاف از
  همین باگِ capping بود) — برایِ عددِ نهایی باید دوباره اجرا گرفت.
- **MAE_Boxes:** قبل از فیکس، مکانیکی سیستماتیک بالاتر از ground truth بود (میانگینِ اختلاف
  ۱.۲۲ باکس) — چند ردیفِ ground truth دقیقاً `۰.۸۰` تکراری داشتند (یادداشتِ خودِ برگه: «قدیمی‌های
  استاپی=۰.۸ تقریبی») که نشان می‌داد محمود برایِ روزهایِ استاپی یک تقریبِ ثابت نوشته، نه اندازه‌گیریِ
  دقیق — این هم با فیکسِ capping باید تاحدِ زیادی حل شود.
- **DayNet_R در برابرِ Reward_R:** دو فرضیه (لیمیتِ گیت‌شده/ورودِ بلافاصله) تقریباً هم‌تراز بودند
  (~۶۲٪ هر دو) — سؤالِ باز هنوز حل‌نشده، باید بعدِ اجرایِ مجدد (با فیکسِ capping) دوباره سنجید.

## نتایجِ دومین اجرایِ واقعی (دورِ دوم، هر سه پنجره — با کدِ v1.1، قبل از فیکسِ v1.2)

این‌بار هر سه فایل واقعاً سه پنجره‌ی متفاوت بودند (تأیید شد: `BoxHigh`/`BoxLow`/`BoxStart_Server`
سه‌تایشان فرق داشت). خلاصه‌ی `compare_ground_truth.py` (کدِ زمانِ اجرا: v1.1 — یعنی MFE/MAE هنوز
رویِ *هر دو* استاپ/TP freeze می‌شدند، فیکسِ v1.2 بعدِ این نتایج اعمال شد):

| پنجره | BreakDir | MFE_Boxes (±0.15) | MAE_Boxes (±0.15) | DayNet_R (لیمیت) | DayNet_R_MarketEntry |
|---|---|---|---|---|---|
| ۰۸۳۰-۰۹۳۰ | ۵۰/۶۰ (۸۳٪) | ۴۱/۵۵ (۷۴٪) | ۲۲/۳۷ (۶۰٪) | ۲۵/۴۰ (۶۲٪) | ۲۹/۴۷ (۶۲٪) |
| ۰۹۰۰-۱۰۰۰ | ۴۳/۶۰ (۷۲٪) | ۳۶/۵۳ (۶۸٪) | ۱۶/۴۱ (۳۹٪) | ۱۷/۲۸ (۶۱٪) | ۲۱/۳۶ (۵۸٪) |
| ۰۹۳۰-۱۰۳۰ | ۳۹/۶۰ (۶۵٪) | ۲۸/۴۸ (۵۸٪) | ۱۶/۴۰ (۴۰٪) | ۸/۲۱ (۳۸٪) | ۱۰/۲۷ (۳۷٪) |

نکته‌هایِ کلیدی:

- **BreakDir:** افتِ نرخِ تطابق از ۰۸۳۰-۰۹۳۰ به ۰۹۳۰-۱۰۳۰ (۸۳٪→۶۵٪) تقریباً کاملاً توضیح داده شد —
  نگاه کن به «سؤالِ بازِ ۲» بالا (شکست‌هایِ دیرتر از ۶۰ دقیقه).
- **MAE_Boxes:** با اینکه v1.1 نسبت به دورِ اول بهتر شده بود (پنجره‌ی ۰۸۳۰-۰۹۳۰: ۳۲٪→۶۰٪)، هنوز
  زیرِ ۵۰٪ برایِ دو پنجره‌یِ دیگر است — بخشی از این هم به همان مسئله‌یِ سؤالِ بازِ ۲ برمی‌گردد (روزهایی
  که مکانیکی None داد اصلاً وارد این مقایسه نمی‌شوند، اما روزهایِ دیگر هم تحتِ‌تأثیرِ الگویِ مشابه‌اند).
- این جدول **قبل از فیکسِ v1.2** (کپ‌شدنِ MFE فقط در استاپ، نه TP) است — انتظار می‌رود بعدِ اجرایِ
  بعدی، ستونِ MFE_Boxes برایِ هر سه پنجره بهبود پیدا کند.

## نتایجِ دورِ سوم (بعدِ v1.2+v1.3، هر سه پنجره، دیتایِ Errante)

| پنجره | BreakDir | MFE_Boxes | MAE_Boxes | DayNet_R |
|---|---|---|---|---|
| ۰۸۳۰-۰۹۳۰ | ۹۵٪ | ۹۳٪ | ۷۳٪ | ۶۰٪ |
| ۰۹۰۰-۱۰۰۰ | ۹۳٪ | ۸۳٪ | ۶۳٪ | ۴۶٪ |
| ۰۹۳۰-۱۰۳۰ | ۹۳٪ | ۷۹٪ | ۷۳٪ | ۴۶٪ |

BreakDir/MFE به سطحِ خوبی رسیدند. یک پیش‌نمایشِ اولیه‌یِ قدمِ ۳ (رویِ همین دیتایِ Errante) هم گرفته
شد — نتیجه فاصله‌یِ قابلِ‌توجهی با تخمینِ چشمی نشان داد (E مستقیم ≈+۰.۰۷R در برابرِ +۰.۲۶R چشمی؛
موتورِ ریورسِ ۸:۳۰ ≈-۰.۰۳R در برابرِ +۱۵.۲R چشمی). این یافته به همراهِ جدولِ بالا به مدیرِ پروژه
گزارش شد.

## نتایجِ دورِ چهارم (اعتبارسنجیِ هم‌فیدِ XChief + فیکسِ v1.4)

خروجیِ XChief (کدِ v1.2+v1.3، قبل از v1.4):

| پنجره | BreakDir | MFE_Boxes | MAE_Boxes | DayNet_R (فرمولِ قدیم) |
|---|---|---|---|---|
| ۰۸۳۰-۰۹۳۰ | ۹۵٪ | ۹۵٪ | ۷۳٪ | ۶۰٪ |
| ۰۹۰۰-۱۰۰۰ | ۹۳٪ | ۸۳٪ | ۶۸٪ | ۴۶٪ |
| ۰۹۳۰-۱۰۳۰ | ۹۲٪ | ۸۱٪ | ۷۳٪ | ۴۶٪ |

تقریباً بدونِ تغییر نسبت به Errante — رد کنند‌ه‌ی فرضیه‌ی «بروکر». بعدِ ریشه‌یابی و فیکسِ v1.4
(تصمیمِ پیاده‌سازیِ ۶)، `DayNet_R` با فرمولِ جدید (شبیه‌سازیِ آفلاین، هنوز رسماً با MT5 تأیید نشده):

| پنجره | DayNet_R — Errante | DayNet_R — XChief |
|---|---|---|
| ۰۸۳۰-۰۹۳۰ | ۹۴٪ | ۹۴٪ |
| ۰۹۰۰-۱۰۰۰ | ۸۵٪ | ۹۰٪ |
| ۰۹۳۰-۱۰۳۰ | ۷۴٪ | ۷۸٪ |

دو پنجره از هدفِ ۸۰٪ عبور کردند؛ ۰۹۳۰-۱۰۳۰ خیلی نزدیک است (باقیمانده عمدتاً نویزِ اندازه‌گیریِ کوچک
رویِ روزهایی با MFE نزدیکِ صفر + یک موردِ شناخته‌شده‌یِ کندلِ پرنوسانِ دوجهته).

⚠️ این جدول با فرمولِ **v1.4** بود (سنجیده با `Reward_R`) — بعداً معلوم شد `Reward_R` معیارِ غلطی
برایِ `DayNet_R` بود. نگاه کن به «نتایجِ دورِ پنجم» پایین برایِ عددِ درست.

## نتایجِ دورِ پنجم (v1.5 — تصحیحِ نهایی، اعتبارسنجی با `compare_ground_truth.py` رسمی)

بعدِ کشفِ ستونِ سیزدهمِ ground truth (`EODResult_Boxes`) و فیکسِ v1.5، `compare_ground_truth.py`
دوباره اجرا شد (رویِ دیتایِ XChief موجود، با ستونِ `DayNet_R_Potential` بازسازی‌شده — هنوز اجرایِ
تازه‌یِ MT5 با کدِ v1.5 نگرفته شده):

| پنجره | `DayNet_R` رسمی در برابرِ ground truthِ درست | `DayNet_R_Potential` در برابرِ `Reward_R` |
|---|---|---|
| ۰۸۳۰-۰۹۳۰ | **۸۷.۲٪** (۴۱/۴۷) | ۹۳.۶٪ |
| ۰۹۰۰-۱۰۰۰ | **۹۲.۳٪** (۳۶/۳۹) | ۸۹.۷٪ |
| ۰۹۳۰-۱۰۳۰ | **۸۶.۵٪** (۳۲/۳۷) | ۷۸.۴٪ |

**هر سه پنجره از هدفِ ۸۰٪ی مدیرِ پروژه عبور کردند.** بندِ ۲ دستورِ مدیرِ پروژه (پایین) برآورده شد.

## نتایجِ دورِ ششم — تأییدِ رسمی با اجرایِ واقعیِ MT5 (نه شبیه‌سازیِ آفلاین) ✅

محمود اسکریپتِ v1.5 را کامپایل و رویِ XChief (هر سه پنجره) اجرا کرد. `compare_ground_truth.py`
رویِ خروجیِ واقعی (نه بازسازیِ آفلاین) اجرا شد — نتیجه **دقیقاً همان اعدادِ دورِ پنجم** بود:

| پنجره | BreakDir | `DayNet_R` رسمی |
|---|---|---|
| ۰۸۳۰-۰۹۳۰ | ۹۵.۰٪ | ۸۷.۲٪ (۴۱/۴۷) |
| ۰۹۰۰-۱۰۰۰ | ۹۳.۳٪ | ۹۲.۳٪ (۳۶/۳۹) |
| ۰۹۳۰-۱۰۳۰ | ۹۱.۷٪ | ۸۶.۵٪ (۳۲/۳۷) |

**تأییدِ کامل — قدمِ ۲ رسماً بسته شد.** خروجیِ خام در `runs/2026-08-23_run6_v15_official/`.

## دستورِ مدیرِ پروژه — قفلِ نهاییِ قدمِ ۳ (بعدِ بررسیِ گزارشِ بالا)

مدیرِ پروژه شش بندی را تصویب/دستور داد:

۱. **تعریفِ جدیدِ شکست (v1.3) تصویب شد ✅** — نیازی به بازبینیِ بیشتر ندارد.
۲. **قدمِ ۳ قفل است تا اعتبارسنجی با هم‌فیدِ XChief انجام شود، با هدفِ `DayNet_R≥۸۰٪`.**
   **وضعیت: برآورده شد ✅ (v1.5).** اکسپورتِ XChief نشان داد اختلافِ بروکر مقصرِ اصلی نبود؛
   ریشه‌یابیِ دقیق‌تر (v1.4) یک سرنخِ نیمه‌درست پیدا کرد که بعداً محمود با کشفِ یک ستونِ گم‌شده در
   فایلِ اصلی (`EODResult_Boxes`) کاملاً تصحیحش کرد (v1.5 — تاریخچه‌ی کامل بالای سند). با
   ground truthِ درست: `DayNet_R` رسمی **۸۷.۲٪/۹۲.۳٪/۸۶.۵٪** رویِ هر سه پنجره — همه بالایِ هدف.
   **تأییدِ رسمی با اجرایِ واقعیِ MT5 هم انجام و کامل شد (دورِ ششم) — همین اعداد را داد.**
۳. **آمارِ نهاییِ قدمِ ۳ (E/WR/...) رویِ دیتایِ Errante گزارش می‌شود**، نه XChief — یعنی XChief فقط
   برایِ اعتبارسنجی (اثباتِ درستیِ منطق) استفاده می‌شود؛ چون دیتاستِ کاملِ ۱۷ماهه‌یِ قابلِ‌اتکا رویِ
   Errante موجود است.
۴. **بعدِ عبورِ اعتبارسنجیِ XChief، یک بررسیِ تشخیصیِ ۱۵روزه برایِ موتورِ ریورس** انجام شود — نمونه‌ای
   از ۱۵ روزِ دارایِ `Rev_Triggered=1` که `Rev_ExitR`/`Rev_MFE` مکانیکی با `Rev_MFE_Boxes` برگه‌یِ
   چشمی مقایسه شود، تا فاصله‌یِ E ریورس (-۰.۰۳ در برابرِ +۱۵.۲) ریشه‌یابی شود.
   **انجام شد ✅ — نتیجه در `docs/Reversal_15Day_Diagnostic.md`.** خلاصه: از ۱۴ روزِ فعال‌شده،
   ۸ روز (۵۷٪ خام) درونِ تلورانس جور بودند؛ ۲ روز (`۰۶-۱۲`, `۰۶-۰۸`) اختلافِ جهت‌معکوس داشتند —
   **هر دو با چکِ چارتِ محمود حل شدند ✅**: هیچ‌کدام واقعاً مالِ لگِ ریورسِ مکانیکی نبوده‌اند
   (اشتباهِ ثبتِ چشمی روی حرکاتِ لگِ مستقیم/رتریس). با کنار گذاشتنِ این دو، تطابقِ واقعی رویِ ۱۲
   روزِ قابلِ‌مقایسه = **۸/۱۲ (۶۷٪)** — موتورِ ریورس هیچ ناهنجاریِ جهت‌معکوسِ واقعی‌ای نداشت.
   میانگینِ `Rev_ExitR` مکانیکی رویِ همین ۱۵ روز = **+۰.۷۵R** — با میانگینِ کلِ ۱۷ماهه (-۰.۰۳R)
   خیلی فرق دارد؛ با این نمونه‌ی کوچک نمی‌شود گفت این فاصله از باگ می‌آید یا از سوگیریِ
   انتخاب/نمونه‌یِ کوچکِ چشمی — نیازمندِ تحلیلِ کاملِ قدمِ ۳.
۵. **تقویمِ اخبار (NewsDay) را خودِ مدیرِ پروژه تأمین می‌کند** — نیازی به ساختنِ آن از این طرف نیست.
   **به‌روزرسانی v1.6:** تصمیمِ نهاییِ مدیرِ پروژه (`Manager_Decision_20260823.md`، بندِ ۴)
   استفاده از تقویمِ داخلیِ MT5 (`CalendarValueHistory`, USD/HIGH) به‌جایِ فایلِ خارجی بود —
   پیاده‌سازی شد (نگاه کن به `NY_DataScript.mq5`). فایلِ دستی فقط پلن B است، اگر عمقِ تاریخیِ
   تقویمِ ترمینال کافی نبود (چکِ خودکار در لاگِ اسکریپت).
۶. **قبل از هر حکمِ نهاییِ E، مدلِ هزینه (اسپرد/اسلیپیجِ پنجره‌یِ NY، طبقِ `SpreadLog` که در سندِ
   اصلی اشاره شده) باید اعمال و جدا گزارش شود.** **در حالِ تکمیل:** مدیرِ پروژه دیتایِ SpreadLog
   واقعی را فرستاد (`data/SpreadLog/` — فرمتِ M1، `Bid/Ask/Spread$`، هر دو بروکر Errante و XChief)
   ولی فقط ۵ روزه (۱۷-۲۱ اوت ۲۰۲۶)، و طبقِ محمود دیتایِ کاملِ قابلِ‌اتکا ~۱ هفته‌ی دیگر طول می‌کشد.
   پیش‌نمایشِ خامِ ۵روزه (نگاه کن به `data/SpreadLog/README.md`): میانگینِ اسپرد در پنجره‌هایِ باکس
   ~$۰.۲۳ (Errante) و ~$۰.۱۷ (XChief). **به‌روزرسانی v1.6:** مدیرِ پروژه تصمیم گرفت تحلیلِ موقتِ
   قدمِ ۳ هم بدونِ هزینه اجرا نشود — یک هزینه‌یِ ثابتِ **۰.۰۶R به‌ازایِ هر لگِ اجراشده** در
   `tools/analyze_ny.py` اعمال و جدا از عددِ خام گزارش می‌شود (نگاه کن به `EXEC_COST_R`). معماریِ
   نهایی (اسپرد کامل + کمیسیونِ قرارداد + اسلیپیجِ ممیزیِ ریل) بعدِ رسیدنِ SpreadLog کامل جایگزین
   می‌شود.

## وضعیت

قدمِ ۱ کامل و **شش بار** در MT5 اجرا شده (آخرین بار: کدِ v1.5 رویِ XChief). قدمِ ۲ **رسماً و کامل
بسته شد**: BreakDir ~۹۲-۹۵٪، `DayNet_R` رسمی ~۸۷-۹۲٪ — هم با شبیه‌سازیِ آفلاین و هم با اجرایِ
واقعیِ MT5 تأیید شد؛ **هدفِ ۸۰٪ی مدیرِ پروژه برآورده شد.**

**قدمِ ۳ی موقت (تأییدِ مدیرِ پروژه، v1.6) — انجام شد ✅.** هر ۷ اجرا (رویِ **Errante**، ۱۷ماهه‌ی
کامل، ۲۰۲۵-۰۳-۲۰..۲۰۲۶-۰۸-۲۱) گرفته شد و `tools/analyze_ny.py` رویشان اجرا شد — نتیجه:
[`runs/2026-08-24_run7_v16_prelim/NY_Standalone_Report_PRELIM.md`](../runs/2026-08-24_run7_v16_prelim/NY_Standalone_Report_PRELIM.md)
(با برچسبِ صریحِ «موقت»، طبقِ دستورِ مدیرِ پروژه).

> نکته‌ی جانبی از این دور: اجرایِ اولِ ۰۸۳۰-۰۹۳۰ یک‌بار با `Bias_Daily`/`EMA_Slope`/`NewsDay` کاملاً
> خالی برگشت (چون چارتِ H1 هیچ‌وقت لود نشده بود — EMA20/H1 نیازمندِ تاریخچه‌ی H1 است، نه فقط M5).
> بعدِ لودِ H1، اجرایِ دوم کاملاً سالم بود. اگر بازِ آینده در MT5 تکرار شود، چارتِ H1 را هم اسکرول کن.

**یافته‌هایِ کلیدیِ گزارشِ موقت:**
- دوئلِ پنجره: `0900-1000` کمی بهتر از `0930-1030` (E خام ۰.۰۷۳ در برابرِ ۰.۰۶۶؛ با هزینه‌یِ
  ۰.۰۶R/لگ هر دو نزدیکِ صفر می‌شوند).
- جاروی استاپ: استاپِ کم‌عمق‌ترِ ۰.۵۰ رویِ هر دو پنجره E خامِ بالاتری داد؛ کیفیتِ منحنی (MaxDD)
  هنوز باید جدا سنجیده شود.
- **یافته‌یِ مهم — بندِ ۴ دستورِ مدیرِ پروژه تأیید شد:** E ریورس رویِ همه‌یِ ۷۲ فعال‌سازیِ ۱۷ماهه
  = **-۰.۰۳۰R**، در برابرِ +۰.۷۵R رویِ نمونه‌ی ۱۵تاییِ چشمی — فرضیه‌یِ سوگیریِ ثبتِ انتخابیِ چشمی
  تأیید شد؛ فرضیه‌یِ ④ (اختلافِ منطقی/باگ) رسماً ابطال می‌شود.
- مقایسه‌ی نهایی با توکیو: هنوز TODO — این ریپو دسترسیِ محلی به `DayBias_History_XAUUSD.csv`ِ
  ریپوی توکیو نداشت؛ اگر لازم شد، آن فایل را جدا فراهم کن.

قبلاً/همچنان معلق:

۱. دیاگنوستیکِ ۱۵روزه‌یِ ریورس (بندِ ۴ دستورِ مدیرِ پروژه) — **انجام شد ✅**، نتیجه در
   [`docs/Reversal_15Day_Diagnostic.md`](Reversal_15Day_Diagnostic.md). خلاصه: از ۱۴ روزِ
   فعال‌شده، ۸ روز (۵۷٪ خام) درونِ تلورانس جور بودند؛ ۲ روز (`۰۶-۱۲`, `۰۶-۰۸`) اختلافِ جهت‌معکوس
   داشتند که **هر دو با چکِ چارتِ محمود حل شدند** (اشتباهِ ثبتِ چشمی، نه باگِ ریورس) — تطابقِ
   اصلاح‌شده رویِ ۱۲ روزِ قابلِ‌مقایسه = ۸/۱۲ (۶۷٪). میانگینِ `Rev_ExitR` مکانیکی رویِ همین ۱۵ روز
   = +۰.۷۵R — با میانگینِ کلِ ۱۷ماهه (-۰.۰۳R) خیلی فرق دارد؛ نتیجه‌گیریِ قطعی نیازمندِ تحلیلِ کاملِ
   قدمِ ۳ست.
۲. منتظرِ دیتایِ کاملِ SpreadLog (~۱ هفته‌ی دیگر، طبقِ محمود) — فرمت شناخته شده، فقط دیتایِ کافی
   نیست. `data/SpreadLog/` دارایِ نمونه‌ی ۵روزه برایِ آشناییِ اولیه‌ست.
۳. بعدِ رسیدنِ دیتایِ کاملِ SpreadLog، آمارِ نهاییِ قدمِ ۳ رویِ Errante (با مدلِ هزینه‌یِ اعمال‌شده)
   تهیه می‌شود.
