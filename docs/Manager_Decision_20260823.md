# پیام مدیر پروژه → لاین NY Box — تصمیم‌ها و ادامه کار
تاریخ: ۲۳ اوت ۲۰۲۶ | در پاسخ به: `Manager_Update_20260823.md`

## تصمیم‌ها

**۱. ادامه کار تأیید می‌شود ✅** — طبق پیشنهاد خودتان:
- اکسپورت تازه با کد **v1.5** روی دیتای کامل ۱۷ماهه **Errante** (اجراهای v1.2/v1.3 موجود در ریپو برای تحلیل استفاده نشود).
- اجرای کامل `tools/analyze_ny.py` (هر ۶ سؤال سند) با برچسب صریح **«موقت — پیش از مدل هزینه نهایی و NewsDay»**. هیچ عددی از این نسخه وارد LP RULES یا حکم نهایی نمی‌شود.
- **جاروی استاپ** {0.5, 0.6, 0.75} موازی اجرا شود.

**۲. تحلیل موقت هم بدون هزینه اجرا نشود — هزینه تقریبی از الان اعمال شود:**
از SpreadLog پنج‌روزه (تحلیل‌شده توسط لاین اصلی): اسپرد هر دو بروکر ثابت و بدون تورم ساعتی است (xChief ~0.17$، Errante ~0.23$؛ پنجره ۸:۳۰-۱۱:۳۰ = بقیه روز؛ EOD فقط دم خفیف). تا رسیدن لاگ کامل، **هزینه ثابت 0.06R به ازای هر لگ اجراشده** اعمال و جدا گزارش شود. با Eهای پیش‌نمایش (~+0.07)، نتایج با و بدون هزینه هر دو در جدول بیاید.

**۳. معماری مدل هزینه نهایی — سه‌جزئی، نه تک‌منبعی:**
- **اسپرد:** از SpreadLog دمو (معتبر — اسپرد قیمت‌گذاری بروکر است نه اجرا؛ لاگ کامل ~۱ هفته دیگر می‌رسد).
- **کمیسیون:** از مشخصات قرارداد بروکر (ورودی دستی).
- **اسلیپیج:** دمو صفرِ کاذب نشان می‌دهد — استفاده نشود. منبع: **ممیزی حساب ریل (executionaudit)**: توزیع اسلیپیج استاپ میانه 0.029R / میانگین 0.072R، به تفکیک ساعت. برچسب: «برآورد از ریل MT4، بازبینی با داده فوروارد». توجه: پنجره ۸:۳۰-۱۰ NY در ممیزی گران‌ترین ساعت اسلیپیج بود — در سناریوی P75/P95 لحاظ شود.

**۴. NewsDay — از تقویم داخلی MT5، نه فایل خارجی:**
توابع خانواده `CalendarValueHistory` در MQL5 (فیلتر: currency=USD، importance=HIGH) ستون NewsDay را مستقیم در اسکریپت اکسپورت پر کنند — پوشش کامل ۱۷ ماه شامل دوره به‌هم‌ریختگی برنامه انتشار پاییز ۲۰۲۵ (شات‌داون) که هر تقویم دستی را آنجا خطاپذیر می‌کند.
**چک اول:** عمق تاریخی تقویم در ترمینال Errante تا 2025-03-17 برسد؛ اگر نرسید اعلام کنید تا CSV از منابع رسمی توسط لاین اصلی ساخته شود (پلن B).

**۵. قاب تفسیر معمای ریورس (برای تحلیل کامل):**
دیاگنوستیک ۱۵روزه منطق موتور را تأیید کرد ✅. فرضیه پیش‌فرض برای فاصله +0.75 چشمی / −0.03 مکانیکی: **ثبت انتخابی چشمی (سوگیری بازمانده)** — چشم فقط ریورس‌های چشمگیر را ثبت کرده، مکانیکی همه فعال‌سازی‌ها را می‌شمرد. در تحلیل کامل، این تفکیک صریح گزارش شود: E ریورس روی همه فعال‌سازی‌ها + توزیع Rev_ExitR. اگر تأیید شد، فرضیه ④ رسماً ابطال می‌شود (سومین ابطال تمیز پروژه — نتیجه معتبر است، شکست نیست).

**۶. یادآوری قیدها:** تعریف‌های فریزشده v1.5 (شامل تعریف جدید شکست: معتبر تا ۱ ساعت مانده به EOD) دیگر دست نمی‌خورند؛ سقف گرید همان ۲ پنجره برتر × ۳ استاپ + ریورس ۸:۳۰؛ نسخه «نهایی» فقط پس از SpreadLog کامل + NewsDay.

## خروجی مورد انتظار این مرحله
`NY_Standalone_Report_PRELIM.md`: هر ۶ سؤال + جاروی استاپ + جدول با/بدون هزینه 0.06R + ستون NewsDay (در صورت موفقیت تقویم MT5) + جدول مقایسه هم‌بازه با توکیو (پایه و D). به‌علاوه فهرست هر تصمیم/ابهامی که برای نسخه نهایی باز می‌ماند.
