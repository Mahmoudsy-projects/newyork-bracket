# NY_Standalone_Report — فازِ ۱، قدمِ ۳

> تولیدشده توسطِ tools/analyze_ny.py — طبقِ ترتیبِ سؤال‌هایِ بندِ ۳ سند.

### دوئلِ پنجره: 0900-1000 در برابرِ 0930-1030

| متریک | 0900-1000 | 0930-1030 |
|---|---|---|
| n | 253.000 | 246.000 |
| E | 0.073 | 0.066 |
| WR | 0.443 | 0.480 |
| Sum | 18.551 | 16.169 |
| MaxDD | -11.127 | -15.643 |
| TTNH_days | 79.000 | 116.000 |

### جاروی استاپ {0.5, 0.6, 0.75}

**TODO** — این جارو با ستون‌هایِ فعلیِ CSV (که فقط برایِ استاپِ ثابتِ ۷۵٪ محاسبه شده‌اند) قابلِ بازسازیِ دقیق نیست، چون مسیرِ کاملِ کندل‌به‌کندل ثبت نشده (فقط MFE/MAE/exitR نهایی). برایِ اجرایِ این بخش: `NY_STOP_DEPTH_PCT` در `NY_DetectionLayer.mqh` را به یک ورودیِ پارامتریک (`InpStopDepthPct`) تبدیل کن، و برایِ دو پنجره‌ی برتر (نتیجه‌ی سؤالِ ۱) با هرکدام از سه مقدار دوباره اجرا بگیر (۶ اجرایِ اضافه). سنجه‌ی دوم طبقِ سند: کیفیتِ منحنی (نه فقط E).

### موتورِ ریورسِ ۸:۳۰

روزهایِ دارایِ ترید (فقط لگِ مستقیم): n=260, E=0.053

نرخِ فعال‌شدنِ ریورس (از کلِ روزهایِ دارایِ شکست): 19.6%

از روزهایِ فعال‌شده: TP=8.3%  Stop=44.4%  (n=72, E ریورسِ تنها=-0.030)


**E کاملِ مستقیم+ریورس (سؤالِ ۳):**

n=261, E=0.055, WR=0.410, Sum=14.229, MaxDD=-10.439


> سؤالِ سند: «پیچیدگیِ دولگی می‌ارزد؟» — این E را با E مستقیمِ تنها (بالا) و با بهترینِ پنجره‌ی سادهِ سؤالِ ۱ مقایسه کن.

### ناهنجاریِ خلاف‌EMA — 0830-0930

| گروه | n | E | WR |
|---|---|---|---|
| هم‌جهتِ EMA20/H1 | 141 | 0.010 | 0.369 |
| خلافِ EMA20/H1 | 119 | 0.104 | 0.454 |

یافته‌ی چشمی: خلاف‌EMA در NY بهتر بود (وارونه‌ی توکیو) — این جدول همان فرضیه را روی دیتایِ مکانیکی می‌سنجد.

### تفکیکِ NewsDay — 0830-0930

| گروه | n | E | WR | Sum |
|---|---|---|---|---|
| روزِ خبری (high-impact USD) | 0 | nan | nan | nan |
| غیرِخبری | 260 | 0.053 | 0.408 | 13.767 |

⚠️ این جدول فقط درست است اگر `--news-calendar` هنگامِ اجرایِ MT5 پر شده باشد (`InpNewsCalendarFile` در NY_DataScript.mq5)؛ وگرنه NewsDay همیشه خالی است.

### ناهنجاریِ خلاف‌EMA — 0900-1000

| گروه | n | E | WR |
|---|---|---|---|
| هم‌جهتِ EMA20/H1 | 138 | 0.009 | 0.399 |
| خلافِ EMA20/H1 | 115 | 0.150 | 0.496 |

یافته‌ی چشمی: خلاف‌EMA در NY بهتر بود (وارونه‌ی توکیو) — این جدول همان فرضیه را روی دیتایِ مکانیکی می‌سنجد.

### تفکیکِ NewsDay — 0900-1000

| گروه | n | E | WR | Sum |
|---|---|---|---|---|
| روزِ خبری (high-impact USD) | 0 | nan | nan | nan |
| غیرِخبری | 253 | 0.073 | 0.443 | 18.551 |

⚠️ این جدول فقط درست است اگر `--news-calendar` هنگامِ اجرایِ MT5 پر شده باشد (`InpNewsCalendarFile` در NY_DataScript.mq5)؛ وگرنه NewsDay همیشه خالی است.

### ناهنجاریِ خلاف‌EMA — 0930-1030

| گروه | n | E | WR |
|---|---|---|---|
| هم‌جهتِ EMA20/H1 | 118 | 0.024 | 0.466 |
| خلافِ EMA20/H1 | 128 | 0.105 | 0.492 |

یافته‌ی چشمی: خلاف‌EMA در NY بهتر بود (وارونه‌ی توکیو) — این جدول همان فرضیه را روی دیتایِ مکانیکی می‌سنجد.

### تفکیکِ NewsDay — 0930-1030

| گروه | n | E | WR | Sum |
|---|---|---|---|---|
| روزِ خبری (high-impact USD) | 0 | nan | nan | nan |
| غیرِخبری | 246 | 0.066 | 0.480 | 16.169 |

⚠️ این جدول فقط درست است اگر `--news-calendar` هنگامِ اجرایِ MT5 پر شده باشد (`InpNewsCalendarFile` در NY_DataScript.mq5)؛ وگرنه NewsDay همیشه خالی است.

### مقایسه‌ی نهایی با توکیو

**TODO** — مسیرِ CSVِ توکیو داده نشد یا پیدا نشد (`--tokyo`). این جدول را با `DayBias_History_XAUUSD.csv`ِ ریپوی RiskManager-EA اجرا کن.
